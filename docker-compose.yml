services:
  tu-app-mvc:
    image: container-asp
    ports:
      - "8084:80"
    env_file:
      - .env
    environment:
      - ConnectionStrings__MySqlConnection=Server=${DB_HOST};Database=${DB_NAME};Uid=${DB_USER};Pwd=${DB_PASS};